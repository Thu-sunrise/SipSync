apiVersion: v1
kind: Service
metadata:
  name: casso-backend-service
  namespace: casso-test
spec:
  type: NodePort
  selector:
    app: casso-backend
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30100