apiVersion: v1
kind: ResourceQuota
metadata:
  name: casso-test-quota
  namespace: casso-test
spec:
  hard:
    requests.cpu: "0.5"
    requests.memory: "700Mi"
    limits.cpu: "1"
    limits.memory: "1.2Gi"